# Contributing

- Keep changes defensive and privacy-first.
- Do not introduce logging of raw passwords.
- Add tests for new logic.
- Run:
  - `pip install -r requirements.txt -r requirements-dev.txt`
  - `pytest -q`
